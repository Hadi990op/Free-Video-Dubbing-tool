# Core dependencies
faster-whisper==1.2.1
deep-translator==1.11.4
edge-tts==7.2.8
gradio_client==2.5.0
flask==3.1.3
gunicorn==26.0.0

# Voice cloning — OpenVoice V2 (primary, fast on CPU) + Coqui XTTS-v2 (fallback)
# OpenVoice V2: tone color conversion, RTF ~2.2 on CPU, works for ALL languages
# XTTS-v2: slower fallback, limited languages
coqui-tts>=0.27.0
transformers>=4.40,<5.0

# OpenVoice V2 dependencies (installed from git in setup.sh)
librosa>=0.10.0
pydub>=0.25.1
wavmark>=0.0.3
eng_to_ipa>=0.0.2
inflect>=7.0.0
unidecode>=1.3.7
pypinyin>=0.50.0
cn2an>=0.5.22
jieba>=0.42.1
langid>=1.1.6

# Audio / ML
# NOTE: torch/torchaudio are pinned to <2.9 in setup.sh because torch 2.9+
# requires the torchcodec native libs which fail to load on CPU-only/minimal
# VMs (missing libnvrtc). Install via setup.sh, not directly here.
torch>=2.0.0,<2.9
torchaudio>=2.0.0,<2.9
numpy>=1.24.0,<3.0
scipy>=1.10.0
soundfile>=0.12.0
scikit-learn>=1.2.0
simple-diarizer>=0.0.13
demucs
