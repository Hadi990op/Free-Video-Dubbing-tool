version: "3.8"

services:
  video-dubber:
    build: .
    ports:
      - "5050:5050"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
